/**
 * Mobile panel switcher tests — component replaced in Dark Atelier UI rebuild.
 * The new bottom nav is part of AppShell; tests will be rewritten once
 * agent-146 finishes creating the new AppShell.
 */
import { describe } from 'vitest';

describe.todo('Mobile panel switcher (replaced by AppShell bottom nav in new UI)');
